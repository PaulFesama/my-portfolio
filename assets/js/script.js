function closeMenu() {
  // Close the navbar collapse after a link is clicked
  $('.navbar-collapse').collapse('hide');
}

// btn clicked 
function scrollToSkills() {
  // Scroll to the skills section
  document.getElementById('skills').scrollIntoView({ behavior: 'smooth' });
}